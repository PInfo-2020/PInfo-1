apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "feedfusion.fullname" . }}-test-connection"
  labels:
    {{- include "feedfusion.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "feedfusion.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
